# Placeholder for later phases. No tests exist yet in Phase 0; this keeps
# `add_subdirectory(tests)` in the root CMakeLists.txt valid and gives
# future phases an obvious place to add a test executable / CTest cases
# (e.g. via FetchContent'd Catch2 or GoogleTest) without touching the root
# CMakeLists.txt structure.
