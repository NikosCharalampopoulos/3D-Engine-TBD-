{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug engine_app",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${command:cmake.launchTargetPath}",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${command:cmake.launchTargetDirectory}",
            "environment": [],
            "externalConsole": true,
            "preLaunchTask": "CMake Build"
        }
    ]
}
