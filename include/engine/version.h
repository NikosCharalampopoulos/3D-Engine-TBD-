#ifndef ENGINE_VERSION_H
#define ENGINE_VERSION_H

// Placeholder public header for later phases. Phase 0 does not yet have an
// "engine" library target (see the note at the top of CMakeLists.txt) --
// this file exists so the include/engine/ layout is in place from the
// start, and so future phases have an obvious spot to add real public API
// headers as the engine library is split out from engine_app.

#define ENGINE_VERSION_MAJOR 0
#define ENGINE_VERSION_MINOR 0
#define ENGINE_VERSION_PATCH 1

#endif  // ENGINE_VERSION_H
